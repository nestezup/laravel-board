# ==============================================
# Coolify 배포용 환경변수 템플릿
# ==============================================
# 아래 값들을 Coolify 환경변수 설정에 복사하여 사용하세요.
# 주석으로 표시된 부분은 실제 값으로 변경해야 합니다.

# ----------------------------------------------
# Application Settings (필수)
# ----------------------------------------------
APP_NAME="Laravel Board"
APP_ENV=production
APP_KEY=base64:wK/6kFZoU/j+hbpaxr6ytpztNMRV5P/cab+QFqILQRw=
# 👆 php artisan key:generate 명령으로 생성된 키를 입력하세요
# 또는 Coolify에서 생성 스크립트 실행: php artisan key:generate --show

APP_DEBUG=false
# ⚠️ 프로덕션에서는 반드시 false로 설정

APP_URL=https://your-domain.com
# 👆 실제 도메인으로 변경하세요 (예: https://board.example.com)

APP_LOCALE=ko
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=ko_KR

APP_MAINTENANCE_DRIVER=file
BCRYPT_ROUNDS=12

# ----------------------------------------------
# Logging
# ----------------------------------------------
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error
# 프로덕션에서는 error 또는 warning 권장

# ----------------------------------------------
# Database Settings (필수 - Coolify MySQL 사용시)
# ----------------------------------------------
DB_CONNECTION=mysql
DB_HOST=mysql-database-tow080k4wssso8kcwwwoc04o
# 👆 Coolify에서 MySQL 서비스를 추가하면 자동으로 mysql 호스트명 사용 가능
# 또는 Coolify가 제공하는 데이터베이스 호스트 주소 입력

DB_PORT=3306
DB_DATABASE=default
# 👆 데이터베이스 이름 (Coolify MySQL 서비스에서 생성한 이름)

DB_USERNAME=mysql
# 👆 Coolify MySQL 사용자명

DB_PASSWORD=04vhIpSwakQf8AXJkzejYFdLLVXJnSdH9FHnUmXOX76MEHmgxrrDWRgy9GQuCrpT
# 👆 Coolify MySQL 비밀번호

# ----------------------------------------------
# Session & Cache
# ----------------------------------------------
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

CACHE_STORE=database
CACHE_PREFIX=

# ----------------------------------------------
# Queue
# ----------------------------------------------
QUEUE_CONNECTION=database
BROADCAST_CONNECTION=log

# ----------------------------------------------
# Filesystem
# ----------------------------------------------
FILESYSTEM_DISK=local

# ----------------------------------------------
# Mail (선택 - 메일 기능 사용시)
# ----------------------------------------------
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# ----------------------------------------------
# Redis (선택 - Redis 사용시)
# ----------------------------------------------
# REDIS_CLIENT=phpredis
# REDIS_HOST=redis
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# ----------------------------------------------
# AWS (선택 - S3 사용시)
# ----------------------------------------------
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false

# ----------------------------------------------
# Vite (프론트엔드 빌드)
# ----------------------------------------------
VITE_APP_NAME="${APP_NAME}"

# ==============================================
# Coolify 설정 가이드
# ==============================================
# 
# 1. Coolify에서 MySQL 데이터베이스 서비스 추가
#    - 데이터베이스명, 사용자명, 비밀번호를 위의 DB_* 변수에 입력
# 
# 2. APP_KEY 생성 방법:
#    - 로컬에서: php artisan key:generate --show
#    - 또는 Coolify 배포 후 컨테이너에서 실행
# 
# 3. 배포 후 실행할 명령 (Coolify Post-deployment Command):
#    php artisan migrate --force
#    php artisan config:cache
#    php artisan route:cache
#    php artisan view:cache
# 
# 4. Dockerfile 경로 설정:
#    docker/8.5/Dockerfile
# 
# 5. 포트 설정:
#    컨테이너 포트: 80
# 
# ==============================================
